# Final Discourse Classifier

This directory contains the final trained model assets. Since training was simulated locally on CPU, actual `.bin` or `.safetensors` model weights are omitted. Run Notebook 08 on a GPU instance to populate the weights.